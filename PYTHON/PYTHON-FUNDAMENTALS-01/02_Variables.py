name = "Khyruddin Hashanulla";
age = 23;

print("My name is " + name + ". And I am " + str(age) + " years old.");

# The above code snippet demonstrates the use of variables in Python.
# Variables are used to store data that can be referenced and manipulated later in the program.
# In this case, we have two variables: 'name' and 'age'. The 'name' variable stores a string value, while the 'age' variable stores an integer value.
# The print statement concatenates the string values and outputs them to the console, demonstrating how to use variables within strings.
# Indentation is important in Python, as it defines the structure and flow of the code. In this example, the indentation is used to separate the comments from the code, making it easier to read and understand.