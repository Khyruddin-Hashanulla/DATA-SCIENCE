# Lambda Functions in Python
"""Lambda functions are small anonymous functions defined using the lambda keyword. They can take any number of
arguments but can only have one expression. The expression is evaluated and returned."""

# Example of a lambda function that adds two numbers
add = lambda x, y: x + y
# Calling the lambda function
result = add(5, 10)
print("The sum is:", result)
